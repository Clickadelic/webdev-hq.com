# Ai-rules
Rules for ai-agents

## Ruleset
* never use jQuery
* always prefer local solutions over cloud services (e.g. Google fonts)
* Prefer Laravel native features
* Use Axios for API calls
* No global React state unless required
